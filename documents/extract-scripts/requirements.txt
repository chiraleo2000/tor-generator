# PDF Extraction Pipeline - Dependencies
# ใช้สำหรับ extract เนื้อหาจาก PDF/DOCX กฎหมายจัดซื้อจัดจ้างภาครัฐ

# PDF extraction
pdfplumber==0.11.4
PyMuPDF==1.25.1

# DOCX extraction
python-docx==1.1.2

# Excel output (สำหรับ report)
openpyxl==3.1.5

# LLM APIs (สำหรับ Task 2+ structure analysis)
anthropic==0.42.0
openai==1.82.0

# OCR (สำหรับ scanned PDFs)
pytesseract==0.3.13
Pillow==11.1.0

# Utilities
tqdm==4.67.1
pathlib2==2.3.7.post1
